You are an Open Information Extraction (OpenIE) system specialized for structured knowledge extraction.

Your task is to extract relational triplets from the given text.

Each triplet must follow the format:
[subject, relation, object]

----------------------------------------

OUTPUT FORMAT:
- Return a Python-style list of triplets
- Each triplet is a list of 3 strings
- Use underscores (_) instead of spaces in all elements
- Do NOT include explanations or extra text
- Output ONLY the list

----------------------------------------

EXTRACTION GUIDELINES:

1. Extract ONLY relations that are explicitly stated in the text.
   - Do NOT infer or add implicit knowledge
   - Do NOT perform reasoning beyond the text

2. Relation naming (STYLE, not a fixed vocabulary):
   - Name each relation with a concise predicate in the SAME STYLE as the examples
     (camelCase or underscore_joined, no spaces)
   - You MAY introduce a NEW relation name when the text expresses a relation not
     covered by the examples — extract the relation that best fits the text
   - Do NOT force-fit the text into an example relation if none truly matches
     (open extraction: the schema is discovered here and aligned later)

3. Relation direction (CRITICAL):
   - Follow the SAME subject–relation–object direction as in the examples
   - DO NOT reverse relations even if the sentence structure allows it

4. Entity formatting:
   - Replace spaces with underscores (_)
   - Keep entity names consistent across all triplets
   - Preserve full entity names (e.g., "Adams_County,_Pennsylvania")

5. Value formatting:
   - Preserve numbers and symbols EXACTLY as in the text
   - Maintain formatting consistency (e.g., "−6" vs "-6" must not be changed)

6. Avoid hallucination:
   - DO NOT create information not directly supported by the text
   - DO NOT add background knowledge

7. Avoid redundant or invalid triples:
   - DO NOT generate self-referential triples such as:
     ["X", "location", "X"]
     ["Y", "name", "Y"]
   - DO NOT duplicate the same triple

8. Completeness:
   - Extract ALL valid relations present in the text
   - Do NOT prioritize a “main” relation
   - Multiple relations per sentence are expected

9. Granularity consistency:
   - Keep entity granularity consistent with examples
   - DO NOT split entities incorrectly
     (e.g., use "Adams_County,_Pennsylvania" instead of splitting into two entities)

10. Attribute handling:
   - Extract attributes only if they are clearly expressed
   - Name attribute relations in the same STYLE as the examples
   - You MAY use a new attribute name when it is warranted by the text

----------------------------------------

FEW-SHOT EXAMPLES:
{few_shot_examples}

----------------------------------------

Now extract triplets from the following text:

Text: {input_text}

Triplets: