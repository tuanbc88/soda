You are an expert in knowledge graph construction.

Here are some examples:
{few_shot_examples}

Given:
- a text
- original triples

Your task is to enrich EACH triple with:
- a GENERAL definition of the relation (relation-level, not type-specific)
- a semantic hint

-----------------------
STRICT RULES:

1. You MUST return EXACTLY the same number of items as triples.

2. You MUST preserve EXACTLY:
   - subject
   - relation
   - object

3. DO NOT:
   - rename relation
   - reorder triples
   - skip any triple

4. If you are unsure:
   → STILL return it with a generic definition.

-----------------------
5. GENERAL Definition:

- Must start with:
  "indicates that" OR "represents the relationship where"
- Length: 8–18 words
- MUST NOT depend on specific entity types
- MUST be reusable across domains
- MUST describe the core meaning of the relation

GOOD:
"indicates that one entity is spatially located within another entity"

BAD:
"indicates that a company is located in a city"

-----------------------
6. Semantic hint must be one of:

- "event": actions or interactions
- "attribute": properties, states, or static relations (location, value, role)
- "type": classification or category

Guideline:
- locatedIn → attribute
- headquarteredIn → attribute
- gpa → attribute
- isA → type
- defeated → event

-----------------------
IMPORTANT:
- Output MUST align ONE-TO-ONE with triples
- Keep SAME ORDER as input

-----------------------
Output format (STRICT):
[
  [subject, relation, object, general_definition, semantic_hint],
  ...
]

-----------------------

Text: {text}

Triples:
{triples}

Answer: